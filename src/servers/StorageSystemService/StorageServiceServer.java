package servers.StorageSystemService;

import java.net.Socket;

public class StorageServiceServer {

    public static byte[] storage(Socket mdSocket, byte[] content){
        return new byte[0];
    }
}
